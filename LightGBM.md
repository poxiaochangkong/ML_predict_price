假定股假定股价遵循这样的数学模型
f(t)+g1(t)+g2(t)+g3(t).....
其中abs(f)>abs(g)，f变化率的绝对值小于g1，g1变化率的绝对值小于g2......
我们期望能够训练这样一个模型，能够拟合出f(t),过滤掉g(t)，

采取这样的验证方式评估这个方法，对每天/每个时间段都训练一个吸收之前历史数据的模型，判断当天/当前时间段的趋势
最后输出混淆矩阵和热力图，以及常见的用以评估模型性能的参数
