以茅台数据为例子，处理从2019开始的5分钟线数据，至今
验证方式，
滑动验证，每次输入历史的5分钟线数据（不含当日），输出当日的日线数据；下一次加入当日数据重新拟合，将每次的日线数据收集起来绘制成散点图并与真实股价曲线比较

输入全局数据拟合后与真是股价数据曲线比较


假定股价遵循这些数学模型

model1+卡尔曼滤波/霍尔特-温特斯指数平滑/SG Filter
多尺度分析，股价为一些函数线性叠加，函数具有周期性，周期递减
f(t)+g1(t)+g2(t)+g3(t).....

其中abs(f)>abs(g)，f变化率的绝对值小于g1，g1变化率的绝对值小于g2......

我们期望能够实现这样一个功能，过滤掉所有的g，拟合出f

model2
几何布朗运动??

model3
均值回归模型

Δx_t=θ*(μ-x_t)Δt+σΔW_t
股价存在价值中心
//如果价值中心存在，不太可能保持为一个常数μ




也许还存在别的model
